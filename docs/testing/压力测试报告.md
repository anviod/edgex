---
layout: default
title: 压力测试报告
description: EdgeX 压力测试报告
---

# 压力测试报告

## 场景

- 模拟 200 设备 × 5000 点位
- 采用批量读取与快照返回

## 结果摘要

- 单包 5000 点位读取（Mock）耗时：≤ 200 ms（见 `TestScheduler_Stress_5000Points`）
- API 快照返回：常量级（微秒级）

## 2026 Q3 SLA 三列对比（Phase B-11）

| 场景 | lag P95 | 内存 drift | 故障隔离 | 说明 |
|------|---------|------------|----------|------|
| **Mock 10k**（Q3 benchmark） | ~7 ms | ~4% | N/A | `q3_10k_tag_benchmark_test.go` |
| **Modbus 仿真器** | <50 ms | — | 6/7 串行 | `modbus_protocol_test.go` |
| **ARMv7** | 待板端 | <8% 目标 | — | `scripts/bench_armv7.sh` 交叉编译就绪 |

## 结论

- 满足“单网段 1000 点位并发读取延迟 ≤ 300 ms”的目标（在真实设备条件下需视网络与设备能力调优）

---

## 2026-07-05 回归压测（ScanEngine v5.2 补丁后）

> 对照 [南向采集通道回归验证测试方案](南向采集通道回归验证测试方案.md) §五 与 [ScanEngine重构测试报告](../TODO/ScanEngine重构测试报告.html) §2.4。

### 执行命令

```bash
make bench-g007          # G007 调度吞吐
make bench-q3            # Q3 10k tag deadline
make test-soak-short     # SLA 短 soak（30s，五 gate）
CGO_ENABLED=0 go test ./internal/core/... -short -count=1
```

日志：[`_run_logs/2026-07-05_regression_bench_*.txt`](_run_logs/)

### 结果

| 场景 | 指标 | 目标 | 实测 | 结果 |
|------|------|------|------|------|
| **G007** 1000 设备 · 1s · mock | 调度吞吐 | ≥950 设备/s | **976 设备/s** | ✅ PASS |
| | 任务成功率 | 100% | 29287/29287，failed=0 | ✅ |
| | scan_miss_deadline | 0 | 0 | ✅ |
| | lag P95 | — | 0.57 ms | — |
| **Q3** 100×100 tag · 1s | 吞吐 | — | **9663 pts/s** | ✅ PASS |
| | lag P95 | <100 ms | 1.08 ms | ✅ |
| | miss_deadline | 0 | 0 | ✅ |
| **Short Soak** 30s | Release Gate | 五 gate 全绿 | PASS | ✅ |
| **core `-short`** | 功能/压测用例 | 全绿 | PASS (~45s) | ✅ |

### 未执行项

| 项 | 原因 |
|----|------|
| F 组 12h / 72h soak | 运维级长跑，需 `SOAK_DURATION=12h` 或 nightly 流水线 |
| Modbus 仿真器 6/7 串行压测（联机） | 无本地 Modbus 模拟器集群 |
| ARMv7 板端 bench | 待 `scripts/bench_armv7.sh` 板端执行 |

**2026-07-05 结论**：代码层压力与 SLA gate **全部 PASS**；G007 较 2026-07-04（947/s WARN）提升至 **976/s** 达标。

---

## 单元测试覆盖率门禁（2026-07-05）

| 项 | 阈值 | 基线 | 实测 | 结果 |
|----|------|------|------|------|
| **`internal/core` 语句覆盖率** | **≥ 80%** | 60.3% | **80.5%** | ✅ **达标** |
| **`internal/core` + `internal/driver` 汇总** | **≥ 80%** | 57.7% | **71.7%** | ⚠️ **未达** |
| 测试命令 | — | `CGO_ENABLED=0 go test ./internal/core/... ./internal/driver/... -short -count=1 -coverprofile=coverage.out` | — | — |
| core `-short` 功能回归 | 全绿 | — | PASS（~47s） | ✅ |
| driver `-short` | 全绿 | — | PASS（~3min） | ✅ |
| `make test-soak-short` | 五 gate 全绿 | — | PASS（~78s） | ✅ |

**说明**：**core ≥80% 门禁通过**；合并 scope 受南向 driver 真实 TCP/会话路径制约仍为 **71.7%**。分包明细见 [ScanEngine重构测试报告 §十一](../TODO/ScanEngine重构测试报告.html) 与 [南向驱动测试报告 §八](南向驱动测试报告.html)。

